/** * @author  */var MultiListBox = ListBox.extend({			_setAllSelected: function (value) {		this.controls.each(function(item){			this.baseSetSelected(item, value);		}, this);	},		clear: function () {		this._setAllSelected(false);		return this;	},		select: function () {		this._setAllSelected(true);		return this;	},		getSelectedItems: function(){		return this.controls.filter(function(item, index){			return this.getSelected(index);		}, this);	},		setText: function (value) {		value = value.split(',');		this.controls.each(function(item){			this.baseSetSelected(item, value.indexOf(item.getText()) >= 0);		}, this);				return this;	},		getText: function () {		return this.getSelectedItems().invoke('getText', []).join(',');	}	});