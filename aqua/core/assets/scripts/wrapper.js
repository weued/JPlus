/** * @author  */var Wrapper = {		allDirections: 'lt t rt l r lb b rb'.split(' '),		toggle: function(target, className, directions, oncreate){		var show = directions !== false;				if (target.hasClass(className)) {						target.query('>[class^="' + className + '-"]')[show ? 'show' : 'hide'](0);				} else if(show){						target.addClass(className);						// 全部方向。			(directions ? directions.split(' ') : this.allDirections).forEach(function(direct){				oncreate(target.append(Dom.create('div', className + '-' + direct)), direct);			});						// 允许移动。			Dom.setMovable(target.dom);					}			}};