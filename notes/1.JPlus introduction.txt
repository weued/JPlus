


JPlus 是一个 Javascript 的类库。它提供了包含基础函数、界面组件、常用业务组件等形式的公共代码。

JPlus 分5个模块:


1.  Core 核心: 包含最底层的通用代码。
2.  Components 组件: 包含常用功能的一些封装。一般地，组件的产生是根据实际需要制定的。
3.  Controls 控件: 包含常用的界面组件。控件是只用于界面的组件。
4   Utils 工具: 包含常用的Javascript代码。比如 添加主页、复制文本等功能。